# 5. 성능 비교: 토크나이저 vs 임베딩 vs 하이브리드

> 아래 지연/비용은 일반적인 검색시스템 특성 기반. **실제 수치는 데이터 규모·벡터 차원·하드웨어·튜닝값에 따라 크게 달라지므로 PoC 벤치마크 권장.**

## 한눈에 비교

| 구분 | 토크나이저(전문) | 임베딩(벡터) | 하이브리드 |
|------|:---:|:---:|:---:|
| 쿼리 지연시간 | 빠름 ⚡ | 중간 | 가장 느림 (둘 다 실행) |
| 인덱싱 비용 | 낮음 | 높음 (벡터화 + 그래프 구축) | 높음 (둘 다) |
| 메모리 사용 | 낮음 | 높음 (HNSW 메모리 상주) | 높음 |
| 저장공간 | 작음 | 큼 (벡터 원본 + 인덱스) | 가장 큼 |
| 정확한 키워드 매칭 | 최고 ✅ | 약함 | 좋음 |
| 의미/동의어 검색 | 안 됨 ❌ | 최고 ✅ | 좋음 |
| 검색 품질(RAG 등) | 보통 | 좋음 | 최고 ✅ |

## 각각의 특성

### 토크나이저(전문) — 가볍고 빠름
- 역색인 "단어→행번호" 룩업이라 매우 빠르고 후보를 정확히 좁힘.
- 인덱스 작고 CPU/메모리 부담 낮음. 벡터화 연산 없음.
- 한계: 정확 일치 필요("자동차"↔"차량" 매칭 안 됨). 한글은 조사 문제까지 겹침.

### 임베딩(벡터) — 똑똑하지만 무거움
- ANN이라 정확 최근접보다 빠르나 토크나이저 룩업보단 느림. 차원↑·데이터↑일수록 느려짐.
- 무거운 지점: 임베딩 모델 추론(인덱싱) + HNSW 그래프 구축·메모리 상주 + 벡터 저장(예 1024차원 float ≈ 4KB/건).
- 강점: 단어 달라도 의미 유사하면 검색. 오타·동의어·문맥에 강함.
- 트레이드오프: `efsearch`/`M`↑ → 정확도(recall)↑ 지연↑ (속도↔정확도 맞바꿈).

### 하이브리드 — 품질 최고, 가장 무거움
- 원칙적으로 두 채널을 다 돌려 합치므로 셋 중 가장 느리고 자원 소모 큼.
- **완화 포인트**: StarRocks의 스칼라 프리필터(WHERE) → ANN 최적화. 필터로 후보를 먼저 줄이면 벡터 검색 대상이 작아져 오히려 빨라질 수 있음(In-Filter 최적화, recall 30%+ 개선 언급 — 단 상용 배포판 문서 기준).
- 품질: 키워드 정확성 + 의미 유연성 결합 → RAG/추천에서 표준.

## "기존 하이브리드"와의 차이 — 어디서 합치느냐

| 방식 | 구조 | 성능 함의 |
|------|------|----------|
| 전통 하이브리드 | 벡터DB(Milvus) + 검색엔진(Elasticsearch) 분리 → 앱에서 RRF 병합 | 시스템 2개 왕복 + 네트워크 + 앱 병합. 운영 복잡, 지연 누적 |
| StarRocks 하이브리드 | 한 엔진에서 벡터+전문+필터 (필터+정렬은 SQL로) | 왕복·데이터 이동 없음, 필터 푸시다운으로 후보 축소 → 지연·운영비 유리 |

> ⚠️ **OSS 4.1.1 주의**: StarRocks가 "한 엔진 안에서 자동 융합(RRF)"까지 해주는 건 상용 배포판(EMR Serverless) 이야기. OSS에서는 recall은 한 엔진에서 하되 **융합·재정렬은 직접 구현**(→ `04-fusion-and-oss-implementation.md`). 그래도 두 시스템 왕복이 없다는 이점은 유효.

## 선택 가이드
- 정확한 키워드/코드/ID 검색, 저비용 → **토크나이저 단독**
- 의미·유사 개념·오타 허용, 한국어 조사 변형에 강해야 → **임베딩 단독**
- 검색 품질 최우선(RAG/추천), 정형 필터 많음 → **하이브리드**
- 비용/지연 최우선, 품질은 적당 → 토크나이저 우선, 부족하면 벡터 추가
